# Invasion Mod Template

Drop this folder into `core.liberation` to add or customize factions and build lists. The structure mirrors the classic Liberation RX template so you can swap sides quickly without editing core scripts. Vanilla-ready presets that drive the new lobby faction selectors live in `../factions` (grouped by `blufor`, `opfor`, and `independent`).
